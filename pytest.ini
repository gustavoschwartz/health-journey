[pytest]
; Default run executes only fast, isolated tests (no network, no DB writes).
; Run the live suite explicitly with: pytest -m live
addopts = -m "not live"
markers =
    live: touches the real database (read-only) and the deployed Railway service
pythonpath = .
